export interface mob {
  id: string,
  title: string,
  bossId:string
}

export interface miniBoss {
  id: string,
  title: string,
  bossId: string,
}

export interface Boss {
  id: string,
  title: string,
}